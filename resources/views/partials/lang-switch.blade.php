{{-- BUTTON PINDAH BAHASA JANEE --}}

{{-- <div class="flex items-center space-x-2 ml-4">
  <a href="{{ route('lang.switch','id') }}"
     class="px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest border transition
     {{ app()->getLocale()==='id' ? 'bg-blue-600 text-white border-blue-600' : 'bg-white text-slate-600 border-slate-300 hover:bg-blue-50' }}">
    ID
  </a>

  <a href="{{ route('lang.switch','en') }}"
     class="px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest border transition
     {{ app()->getLocale()==='en' ? 'bg-blue-600 text-white border-blue-600' : 'bg-white text-slate-600 border-slate-300 hover:bg-blue-50' }}">
    EN
  </a>
</div> --}}
